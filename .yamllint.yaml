extends: default

rules:
    braces:
        max-spaces-inside: 1
    comments: disable
    comments-indentation: disable
    document-end: disable
    document-start: disable
    line-length: disable
